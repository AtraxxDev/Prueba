---
layout: layout-page.njk
title: Pagina de blog
description: Meta descripción de la pagina de blog
---

<h1>{{ title }}</h1>
<h2>Articulos del Blog ordenados acendentemente</h2>
<ul>
    {% for post in  collections.blog %}
        <li>
            <a href="{{ post.url }}"> {{ post.data.title }} - {{post.data.date}}</a> 
        </li>
    {% endfor %}
</ul>
<hr>
<h2>Articulos del Blog ordenados decendentemente</h2>
<ul>
    {% for post in  collections.blog | reverse %}
        <li>
            <a href="{{ post.url }}"> {{ post.data.title }} - {{post.data.date}}</a> 
        </li>
    {% endfor %}
</ul>
